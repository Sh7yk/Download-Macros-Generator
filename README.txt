The script will generate a macro that can be placed in an MS Office document. The macro will download your payload and run it in the background. Modify the script to specify the IP, port, and file name of the payload.
